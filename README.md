# Script de scrapping avec puppeteer

## Installation

`npm i puppeteer`
`npm i puppeteer-extra`
`npm i puppeteer-extra-plugin-stealth`

## Utilisation

Pour lancer le scraping il faut executer :
`node nom_du.js`
